class Question {
  String id;
  String question;
  // String email;
  // String mobileNo;
  // String comment;

  // User(this.id, this.name, this.email, this.mobileNo, this.comment);
  Question(this.id, this.question);
}
