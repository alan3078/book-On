class User {
  String id;
  String name;
  // String email;
  // String mobileNo;
  // String comment;

  // User(this.id, this.name, this.email, this.mobileNo, this.comment);
  User(this.id, this.name);
}
